// Palera de cores que será usada
@import '../palettes/blue-indigo'

colors = {
  'darkPrimary': darkPrimary
  'primary': primary,
  'lightPrimary': lightPrimary,
  'textIcons': textIcons,
  'accent': accent,
  'primaryText': primaryText,
  'secundaryText': secundaryText,
  'divider': divider
}

header = {
  height: 100px
}

main = {
  content: {
    order: 2
    backgroundColor: accent
  },
  nav: {
    order: 1,
    width: 300px
    backgroundColor: accent
  },
  aside: {
    order: 3,
    width: 300px
    backgroundColor: accent
  },
}

footer = {
  height: 50px
}